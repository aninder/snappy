-if question.errors
  %ul
    -question.errors.messages.each do |field,error|
      %li
        =field
        -error.each do |e|
          =e

=form_for([current_user, question]) do |f|
  =f.label :question
  =f.text_field :ques
  =f.submit submit, class:"btn btn-info"
